---
import { ClientRouter } from 'astro:transitions';
import CaseNav from '../../components/case/CaseNav.astro';
import CaseSliceImage from '../../components/case/CaseSliceImage.astro';
import slice01 from '../../assets/images/cases/filmaze/slice-01.png';
import slice02 from '../../assets/images/cases/filmaze/slice-02.png';
import slice03 from '../../assets/images/cases/filmaze/slice-03.png';
import slice04 from '../../assets/images/cases/filmaze/slice-04.png';
import slice05 from '../../assets/images/cases/filmaze/slice-05.png';
import slice06 from '../../assets/images/cases/filmaze/slice-06.png';
import slice07 from '../../assets/images/cases/filmaze/slice-07.png';
import slice08 from '../../assets/images/cases/filmaze/slice-08.png';
import slice09 from '../../assets/images/cases/filmaze/slice-09.png';
import slice10 from '../../assets/images/cases/filmaze/slice-10.png';
import slice11 from '../../assets/images/cases/filmaze/slice-11.png';
import slice12 from '../../assets/images/cases/filmaze/slice-12.png';
import slice13 from '../../assets/images/cases/filmaze/slice-13.png';

const slices = [
  slice01,
  slice02,
  slice03,
  slice04,
  slice05,
  slice06,
  slice07,
  slice08,
  slice09,
  slice10,
  slice11,
  slice12,
  slice13,
];
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {/* Критический фон инлайном — чёрный уже на ПЕРВОЙ отрисовке, без вспышки
        белого (бандл стилей подключается позже). is:inline = не хойстить. */}
    <style is:inline>
      html,
      body {
        margin: 0;
        background: #000;
      }
      html {
        color-scheme: dark;
      }
    </style>
    <meta
      name="description"
      content="Онлайн-кинотеатр Filmaze: визуальный кейс мобильного приложения."
    />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>Онлайн-кинотеатр Filmaze</title>
    <ClientRouter fallback="swap" />
  </head>
  <body>
    <CaseNav variant="floating" />
    <main aria-label="Онлайн-кинотеатр Filmaze">
      {
        slices.map((slice, index) => (
          <CaseSliceImage
            src={slice}
            alt={`Filmaze case slice ${String(index + 1).padStart(2, '0')}`}
            eager={index === 0}
          />
        ))
      }
    </main>
  </body>
</html>

<style is:global>
  html,
  body {
    margin: 0;
    min-width: 320px;
    background: #000;
  }

  html {
    color-scheme: dark;
    scrollbar-color: rgba(148, 148, 148, 0.58) transparent;
    scrollbar-width: thin;
  }

  body {
    overflow-x: hidden;
  }

  main {
    width: 100%;
    background: #000;
  }

  img {
    display: block;
    width: 100%;
    height: auto;
    margin: 0;
    padding: 0;
    border: 0;
    image-rendering: auto;
  }

  ::-webkit-scrollbar {
    width: 10px;
    height: 10px;
    background: transparent;
  }

  ::-webkit-scrollbar-track {
    background: transparent;
  }

  ::-webkit-scrollbar-thumb {
    min-height: 48px;
    border: 3px solid transparent;
    border-radius: 999px;
    background: rgba(148, 148, 148, 0.58);
    background-clip: content-box;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: rgba(188, 188, 188, 0.72);
    background-clip: content-box;
  }
</style>
