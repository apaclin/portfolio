---
title: "Example case"
description: "A short walkthrough of an example project."
date: 2026-06-01
order: 1
---

import FlowDiagram from '../../components/diagrams/flow/FlowDiagram.astro';
import { traditionalAp } from '../../components/diagrams/flow/examples/traditional-ap';
import Diagram from '../../components/diagrams/Diagram.astro';
import Hotspots from '../../components/diagrams/Hotspots.astro';
import Carousel from '../../components/diagrams/Carousel.astro';
import Image from '../../components/Image.astro';
import dashboardImg from '../../assets/images/dashboard.png';
import screen1 from '../../assets/images/screen-1.png';
import screen2 from '../../assets/images/screen-2.png';

## Overview

This is a placeholder case study written in **MDX**. It demonstrates that the
`cases-en` collection loads, validates against the schema, and renders.

<FlowDiagram data={traditionalAp} />

### Static version (Figma SVG)

<Diagram
  name="traditional-ap"
  alt={{ en: 'Traditional AP — static diagram', ru: 'Традиционный АР — статичная схема' }}
  caption={{ en: 'Same flow, exported from Figma as SVG (Scenario A). Click to zoom.', ru: 'Тот же поток, экспортирован из Figma в SVG (Сценарий A). Клик — увеличить.' }}
  zoomable
/>

### Annotated screenshot (Hotspots)

<Hotspots
  src={dashboardImg}
  alt="Invoice review dashboard"
  hotspots={[
    { x: 28, y: 34, title: "Risk Score", description: "Anomaly confidence for the selected invoice." },
    { x: 72, y: 58, title: "Vendor Panel", description: "Vendor history and context." }
  ]}
/>

### Screens (Carousel)

<Carousel
  label="Product screens"
  items={[
    { src: screen1, alt: "Dashboard", caption: "Main dashboard" },
    { src: screen2, alt: "Invoice review", caption: "Invoice review flow" }
  ]}
/>

### Zoomable image

<Image
  src={screen1}
  alt="Dashboard screen"
  caption="Hover shows a zoom cursor — click to open the lightbox."
/>

## Outcome

Replace this content with the real story: problem, process, result.
